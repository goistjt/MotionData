language: python
sudo: true
python:
    - "3.5"
before_install:
    - export PYTHONPATH='.'
install:
    - cd flask-server
    - pip install -r requirements.txt
script:
    - python -m unittest discover -p "test_kinematics.py"
