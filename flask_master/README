For running the flask server locally:
    * change CONFIG_PATH to use local_config.ini in python_mysql_dbconfig.py

For running the flask server on the six-dof server:
    * change CONFIG_PATH to use server_config.ini in python_mysql_dbconfig.py